# <%= name %> | Developer Profile

## 🧑‍💻 Основная информация
- **Возраст:** <%= age %>
- **Уровень:** <%= level %>
- **Опыт:** <%= experience %>

## 🛠 Технологический стек
<% stack.forEach(tech => { %>
- <%- tech %>
<% }); %>

## 📊 Распределение навыков
```mermaid
pie
    title Распределение навыков
    <% stack.forEach((tech, index) => { %>
    "<%= tech %>" : <%= (index + 1) * 15 %>
    <% }); %>
📅 Последнее обновление
<%= new Date().toLocaleDateString('ru-RU', {
weekday: 'long',
year: 'numeric',
month: 'long',
day: 'numeric'
}) %>
### 🔍 Особенности этого шаблона:
1. **Поддержка всех полей анкеты**:
   - Использует `name`, `age`, `level`, `experience`, `stack`
   - Автоматически адаптируется к количеству технологий

2. **Визуализация данных**:
   - Диаграмма Mermaid с динамическими значениями
   - Красивое форматирование даты

3. **Защита от ошибок**:
   - Тег `<%-` для безопасного вывода
   - Универсальное форматирование

4. **Поддержка русского языка**:
   - Локализованные названия месяцев/дней
   - Кириллические заголовки

### 🛠 Как создать файл:
1. Перейдите в папку проекта:
```bash
cd duet-profile-cli
